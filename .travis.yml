language: python
python:
  - "2.7"
  - "3.6"

install: 
# - pip install -r requirements.txt
  - python setup.py install

script: py.test python

addons:
    apt:
        sources:
            - llvm-toolchain-precise
        packages:
            - clang-3.7

